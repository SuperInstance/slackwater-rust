[package]
name = "lattice-core"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
description = "High-performance Eisenstein lattice structure for spatial coordination in game worlds"
repository.workspace = true
homepage = "https://github.com/SuperInstance/slackwater-rust"
keywords = ["lattice", "eisenstein", "spatial", "grid", "slackwater"]
categories = ["game-development", "mathematics"]

[dependencies]
serde = { workspace = true }

[dev-dependencies]
criterion = "0.5"
approx = "0.5"
serde_json = { workspace = true }

[[bench]]
name = "snap_bench"
harness = false
