[package]
name = "flux-core"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
description = "Foundation crate for the Slackwater game system — flux state management, event routing, and lifecycle orchestration"
repository.workspace = true
homepage = "https://github.com/SuperInstance/slackwater-rust"
keywords = ["game", "flux", "state-management", "event-routing", "slackwater"]
categories = ["game-development", "game-engines"]

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }

[dev-dependencies]
criterion = "0.5"

[[bench]]
name = "packing"
harness = false
